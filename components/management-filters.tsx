"use client";
import type { Batch } from "@/lib/batches"; import type { Branch } from "@/lib/branches";
export function ManagementFilters({ branches, batches, branchId, batchId, search, onBranchChange, onBatchChange, onSearch, searchPlaceholder = "Search" }: { branches: Branch[]; batches: Batch[]; branchId: number; batchId: number; search: string; onBranchChange: (id: number) => void; onBatchChange: (id: number) => void; onSearch: (value: string) => void; searchPlaceholder?: string }) { const visibleBatches = batches.filter((batch) => !branchId || batch.branchId === branchId); return <div className="flex flex-col gap-3 border-b border-[#eeeae3] p-4 lg:flex-row"><input value={search} onChange={(event) => onSearch(event.target.value)} placeholder={searchPlaceholder} className="flex-1 rounded-lg border border-[#dcdad4] bg-[#fdfcf9] px-3 py-2.5 text-sm" /><select value={branchId} onChange={(event) => { onBranchChange(Number(event.target.value)); onBatchChange(0); }} className="rounded-lg border border-[#dcdad4] bg-[#fdfcf9] px-3 py-2.5 text-sm"><option value={0}>All branches</option>{branches.map((branch) => <option key={branch.id} value={branch.id}>{branch.name}</option>)}</select><select value={batchId} onChange={(event) => onBatchChange(Number(event.target.value))} className="rounded-lg border border-[#dcdad4] bg-[#fdfcf9] px-3 py-2.5 text-sm"><option value={0}>All batches</option>{visibleBatches.map((batch) => <option key={batch.id} value={batch.id}>{branches.find((branch) => branch.id === batch.branchId)?.name || "Branch"} | {batch.batchName}</option>)}</select></div>; }
