"use client";
import Link from "next/link"; import { ArrowLeft } from "lucide-react"; import { useEffect, useState } from "react"; import { useRouter } from "next/navigation"; import { MembershipForm } from "@/components/membership-form"; import { Navbar } from "@/components/navbar"; import { Sidebar } from "@/components/sidebar"; import { getBranches, type Branch } from "@/lib/branches"; import { getStudents, type Student } from "@/lib/students"; import { createMembership } from "@/lib/memberships";
export default function NewMembershipPage() { const router = useRouter(); const [students, setStudents] = useState<Student[]>([]); const [branches, setBranches] = useState<Branch[]>([]); useEffect(() => { Promise.all([getStudents("", 0, 100), getBranches()]).then(([studentPage, branchList]) => { setStudents(studentPage.content); setBranches(branchList); }); }, []); return <Shell><Link href="/memberships" className="mb-6 inline-flex items-center gap-2 text-sm text-[#b3b3b3]"><ArrowLeft size={16} />Back to memberships</Link><h1 className="mb-8 text-3xl font-semibold text-white">Add membership</h1><MembershipForm students={students} branches={branches} onSubmit={async (payload) => { const created = await createMembership(payload); router.push(`/memberships/${created.id}`); }} /></Shell>; }
function Shell({ children }: { children: React.ReactNode }) { return <div className="min-h-screen"><Sidebar /><div className="lg:pl-[272px]"><Navbar /><main className="max-w-5xl d7-page">{children}</main></div></div>; }
