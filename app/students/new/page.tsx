"use client";
import Link from "next/link"; import { ArrowLeft } from "lucide-react"; import { useEffect, useState } from "react"; import { useRouter } from "next/navigation"; import { Navbar } from "@/components/navbar"; import { Sidebar } from "@/components/sidebar"; import { StudentForm } from "@/components/student-form"; import { getBranches, type Branch } from "@/lib/branches"; import { createStudent } from "@/lib/students";
export default function NewStudentPage() { const router = useRouter(); const [branches, setBranches] = useState<Branch[]>([]); useEffect(() => { getBranches().then(setBranches); }, []); return <Shell><Link href="/students" className="mb-6 inline-flex items-center gap-2 text-sm text-slate-500"><ArrowLeft size={16} />Back to students</Link><h1 className="mb-8 text-3xl font-semibold text-slate-900">Add student</h1><StudentForm branches={branches} submitLabel="Create student" onSubmit={async (payload) => { const created = await createStudent(payload); router.push(`/students/${created.id}`); }} /></Shell>; }
function Shell({ children }: { children: React.ReactNode }) { return <div className="min-h-screen"><Sidebar /><div className="lg:pl-[272px]"><Navbar /><main className="d7-page">{children}</main></div></div>; }
