"use client";
import Link from "next/link"; import { ArrowLeft } from "lucide-react"; import { useParams, useRouter } from "next/navigation"; import { useEffect, useState } from "react"; import { Navbar } from "@/components/navbar"; import { Sidebar } from "@/components/sidebar"; import { StudentForm } from "@/components/student-form"; import { getBranches, type Branch } from "@/lib/branches"; import { getStudent, updateStudent, type Student } from "@/lib/students";
export default function EditStudentPage() { const params = useParams<{ id: string }>(); const router = useRouter(); const [student, setStudent] = useState<Student | null>(null); const [branches, setBranches] = useState<Branch[]>([]); useEffect(() => { Promise.all([getStudent(params.id), getBranches()]).then(([item, branchList]) => { setStudent(item); setBranches(branchList); }); }, [params.id]); return <Shell><Link href={`/students/${params.id}`} className="mb-6 inline-flex items-center gap-2 text-sm text-slate-500"><ArrowLeft size={16} />Back to profile</Link><h1 className="mb-8 text-3xl font-semibold text-slate-900">Edit student</h1>{student && <StudentForm initialStudent={student} branches={branches} submitLabel="Save changes" onSubmit={async (payload) => { await updateStudent(params.id, payload); router.push(`/students/${params.id}`); }} />}</Shell>; }
function Shell({ children }: { children: React.ReactNode }) { return <div className="min-h-screen"><Sidebar /><div className="lg:pl-[272px]"><Navbar /><main className="max-w-5xl d7-page">{children}</main></div></div>; }
