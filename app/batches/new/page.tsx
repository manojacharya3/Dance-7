"use client";
import Link from "next/link"; import { ArrowLeft } from "lucide-react"; import { useEffect, useState } from "react"; import { useRouter } from "next/navigation"; import { BatchForm } from "@/components/batch-form"; import { Navbar } from "@/components/navbar"; import { Sidebar } from "@/components/sidebar"; import { getBranches, type Branch } from "@/lib/branches"; import { createBatch } from "@/lib/batches"; import { getInstructors, type Instructor } from "@/lib/instructors";
export default function NewBatchPage() { const router = useRouter(); const [branches, setBranches] = useState<Branch[]>([]); const [instructors, setInstructors] = useState<Instructor[]>([]); useEffect(() => { Promise.all([getBranches(), getInstructors()]).then(([branchList, team]) => { setBranches(branchList); setInstructors(team.content); }); }, []); return <Shell><Link href="/batches" className="mb-6 inline-flex items-center gap-2 text-sm text-slate-500"><ArrowLeft size={16} />Back to batches</Link><h1 className="mb-8 text-3xl font-semibold text-slate-900">Add batch</h1><BatchForm branches={branches} instructors={instructors} onSubmit={async (payload) => { const created = await createBatch(payload); router.push(`/batches/${created.id}`); }} /></Shell>; }
function Shell({ children }: { children: React.ReactNode }) { return <div className="min-h-screen"><Sidebar /><div className="lg:pl-[272px]"><Navbar /><main className="max-w-5xl d7-page">{children}</main></div></div>; }
