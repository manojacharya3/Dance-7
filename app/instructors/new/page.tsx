"use client";
import Link from "next/link"; import { ArrowLeft } from "lucide-react"; import { useEffect, useState } from "react"; import { useRouter } from "next/navigation"; import { InstructorForm } from "@/components/instructor-form"; import { Navbar } from "@/components/navbar"; import { Sidebar } from "@/components/sidebar"; import { getBranches, type Branch } from "@/lib/branches"; import { createInstructor } from "@/lib/instructors";
export default function NewInstructorPage() { const router = useRouter(); const [branches, setBranches] = useState<Branch[]>([]); useEffect(() => { getBranches().then(setBranches); }, []); return <Shell><Link href="/instructors" className="mb-6 inline-flex items-center gap-2 text-sm text-[#667078]"><ArrowLeft size={16} />Back to instructors</Link><h1 className="mb-8 text-3xl font-semibold text-[#18232b]">Add instructor</h1><InstructorForm branches={branches} onSubmit={async (payload) => { const created = await createInstructor(payload); router.push(`/instructors/${created.id}`); }} /></Shell>; }
function Shell({ children }: { children: React.ReactNode }) { return <div className="min-h-screen bg-[#faf9f6]"><Sidebar /><div className="lg:pl-64"><Navbar /><main className="max-w-5xl px-6 py-8 lg:px-10">{children}</main></div></div>; }
