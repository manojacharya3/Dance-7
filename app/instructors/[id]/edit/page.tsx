"use client";
import Link from "next/link"; import { ArrowLeft } from "lucide-react"; import { useParams, useRouter } from "next/navigation"; import { useEffect, useState } from "react"; import { InstructorForm } from "@/components/instructor-form"; import { Navbar } from "@/components/navbar"; import { Sidebar } from "@/components/sidebar"; import { getBranches, type Branch } from "@/lib/branches"; import { getInstructor, updateInstructor, type Instructor } from "@/lib/instructors";
export default function EditInstructorPage() { const params = useParams<{ id: string }>(); const router = useRouter(); const [item, setItem] = useState<Instructor | null>(null); const [branches, setBranches] = useState<Branch[]>([]); useEffect(() => { Promise.all([getInstructor(params.id), getBranches()]).then(([instructor, branchList]) => { setItem(instructor); setBranches(branchList); }); }, [params.id]); return <Shell><Link href={`/instructors/${params.id}`} className="mb-6 inline-flex items-center gap-2 text-sm text-slate-500"><ArrowLeft size={16} />Back to instructor</Link><h1 className="mb-8 text-3xl font-semibold text-slate-900">Edit instructor</h1>{item && <InstructorForm initial={item} branches={branches} onSubmit={async (payload) => { await updateInstructor(params.id, payload); router.push(`/instructors/${params.id}`); }} />}</Shell>; }
function Shell({ children }: { children: React.ReactNode }) { return <div className="min-h-screen"><Sidebar /><div className="lg:pl-[272px]"><Navbar /><main className="max-w-5xl d7-page">{children}</main></div></div>; }
