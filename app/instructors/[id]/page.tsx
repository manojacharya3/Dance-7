"use client";
import Link from "next/link"; import { ArrowLeft, Pencil } from "lucide-react"; import { useParams } from "next/navigation"; import { useEffect, useState } from "react"; import { Navbar } from "@/components/navbar"; import { Sidebar } from "@/components/sidebar"; import { getInstructor, type Instructor } from "@/lib/instructors";
export default function InstructorViewPage() { const params = useParams<{ id: string }>(); const [item, setItem] = useState<Instructor | null>(null); const [error, setError] = useState(""); useEffect(() => { getInstructor(params.id).then(setItem).catch((e) => setError(e instanceof Error ? e.message : "Unable to load instructor.")); }, [params.id]); return <Shell><Link href="/instructors" className="mb-6 inline-flex items-center gap-2 text-sm text-slate-500"><ArrowLeft size={16} />Back to instructors</Link>{error ? <p className="rounded-lg bg-red-50 p-4 text-sm text-red-700">{error}</p> : !item ? <p className="text-sm text-slate-500">Loading instructor...</p> : <><div className="mb-8 flex items-end justify-between"><div><p className="mb-2 text-sm font-semibold text-violet-700">Instructor profile</p><h1 className="text-3xl font-semibold text-slate-900">{item.firstName} {item.lastName}</h1></div><Link href={`/instructors/${item.id}/edit`} className="flex items-center gap-2 rounded-lg bg-violet-600 px-4 py-2.5 text-sm font-semibold text-white"><Pencil size={16} />Edit</Link></div><section className="grid gap-5 rounded-xl border border-slate-200/80 bg-white p-6 sm:grid-cols-2"><Detail label="Email" value={item.email} /><Detail label="Phone" value={item.phone || "Not set"} /><Detail label="Specialization" value={item.specialization || "Not set"} /><Detail label="Status" value={item.active ? "Active" : "Inactive"} /></section></>}</Shell>; }
function Detail({ label, value }: { label: string; value: string }) { return <div><p className="text-xs font-semibold uppercase tracking-[0.1em] text-slate-400">{label}</p><p className="mt-1 text-sm text-slate-700">{value}</p></div>; }
function Shell({ children }: { children: React.ReactNode }) { return <div className="min-h-screen"><Sidebar /><div className="lg:pl-[272px]"><Navbar /><main className="max-w-5xl d7-page">{children}</main></div></div>; }